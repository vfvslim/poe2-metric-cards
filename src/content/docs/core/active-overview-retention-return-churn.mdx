---
title: PoE2 Active Overview Retention / Return / Churn
description: What do retention_users, return_users, and churn_users mean on PoE2 active overview?
---

import MetricMeta from '../../../components/MetricMeta.astro';
import { Aside } from '@astrojs/starlight/components';

<MetricMeta
	status="stubbed"
	module="Core · Databrain"
	table="app_poe2_active_overview_d"
	source="hard-coded 0"
	grain="date × country × os"
	notThis="a real KPI — column exists ≠ implemented"
/>

<Aside type="caution" title="Not populated">
	These three columns are hard-coded to `0` in the insert. Do not report them as
	retention, return, or churn.
</Aside>

## 业务问题 BUSINESS QUESTION

What do `retention_users` / `return_users` / `churn_users` mean on PoE2 active overview?

## 指标定义

Columns exist on `app_poe2_active_overview_d`, but they are **not real metrics yet**. Values are always `0`.

## 计算公式

```sql
0 as retention_users,
0 as return_users,
0 as churn_users
```

No real calculation.

## 口径要素

### 分子 Numerator

None (hard-coded `0`).

### 分母 Denominator

None.

### 粒度 Grain

One row per `dtstatdate`, country, os (same table as DAU; not meaningful while always 0).

## 过滤条件 FILTERS

- `dtstatdate = '{{ ds }}'`
- If someone asks what these columns are on active overview: **not populated / stubbed**.

## 维度

- `dtstatdate`
- `country_code`
- `os`

## 踩坑与注意 CAVEATS

- Status: **stubbed** — not a real KPI. Column exists ≠ metric is implemented.
- Real register retention → [Register retention D1](/poe2-metric-cards/core/register-retention-d1/) (`active_users_d2`).
- Payer retention / return / churn → in-game pay bitmap ([In-game pay](/poe2-metric-cards/core/ingame-pay-amount/)).
- League return → [League overview](/poe2-metric-cards/league/overview/).

## SQL 模式

`app_poe2_active_overview_d.sql` hard-codes these three fields to `0` during insert.

## YAML

```yaml
id: active_overview_retention_return_churn
aliases: [retention_users, return_users, churn_users]
game: poe2
domain: databrain
status: stubbed
tables:
  - poe2_pc.app_poe2_active_overview_d
sql:
  - Path of Exile 2/poe2_pc_databrain/app_poe2_active_overview_d.sql
dag:
  - databrain_poe2_d_v0
grain: date × country × os
```
